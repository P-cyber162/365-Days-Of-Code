const userInfo: [number, string, boolean] = [ 123, 'Prince', true ];

const userID = userInfo[0];
const userName = userInfo[1];
const activeStatus = userInfo[2];

console.log(`${userID}, ${userName}, ${activeStatus}`)